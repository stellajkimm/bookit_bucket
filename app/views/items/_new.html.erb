	<%= form_for [@bucket, @item] do |f| %>
	  <%= f.text_field :name, placeholder: "new item", class: "col-sm-6" %>
	  <%= f.text_field :location, placeholder: "location", class: "col-sm-4" %>
	  <%= f.submit "ADD ITEM", class: "btn btn-default" %>
	<% end %>