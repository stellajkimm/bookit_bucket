<div class="container">
	<%= @bucket.name %> by <%= username(@bucket.user_id) %>
	<ul>
		<% @items.each do |item| %>
			<li><%= item.name %></li>
		<% end %>
	</ul>
</div>
<% @bucket.categories.each do |cat| %>
	<%= cat.name %>
<% end %>