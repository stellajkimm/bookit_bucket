<div class="container">

	<%= render partial: "carousel" %>
	<div class="inner-container white trocci">
		<h1>Bookit Bucket.. lalala something something</h1>
		<h2>This is where I write things that make this website prettier. More words.  This will be filled with another sentence.</h2>
	</div>

	<% @buckets.each do |bucket| %>
		<%= render partial: "bucket", locals: { bucket: bucket} %>
	<% end %>
	<% if @buckets.size % 3 != 0 %>
		<% (3-(@buckets.size % 3)).times do %>
			<div class="bucket col-sm-4 col-xs-6 yellow<%= rand(1..5) %>"></div>
		<% end %>
	<% end %>
</div>