	<%= form_for [@bucket, @item] do |f| %>
	  <%= f.text_field :name, placeholder: "new item", size: 46 %>
	  <%= f.text_field :location, placeholder: "location" %>
	  <%= f.submit "Submit New Item", class: "btn-default" %>
	<% end %>